# Copy this file to `.env` and fill in your key. `.env` is gitignored.
# Free account at https://elevenlabs.io → Profile → API key.
ELEVENLABS_API_KEY=

# Optional fallback if you use OpenAI TTS instead:
# OPENAI_API_KEY=
