# Next Task (do this now)
Goal: make Admin able to create the minimum data needed for real Parent + Tutor flows.

## Admin-web MVP screens
1) Admin: Schools/Cohorts/Classes (create + list)
2) Admin: Users (create teacher/student/parent)
3) Admin: Link Student -> Class, Parent -> Student
4) Admin: Seed/Dev tools page (only in dev) to run /api/test/seed/*
5) Verify Parent-web dashboard shows linked children + data

## Definition of done
- Fresh DB -> admin can create everything via UI
- Parent can log in and see their child dashboard + attendance/grades
- Tutor session can be created and replied to (at least one thread)
