name: classmate_mobile
description: "ClassMate mobile app for students, teachers, and parents."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.7+121

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter_localizations:
    sdk: flutter
  video_player: ^2.9.2
  video_trimmer: ^5.0.0
  just_audio: ^0.9.46
  pdfrx: ^2.2.24
  syncfusion_flutter_pdfviewer: ^33.1.49
  dio: ^5.7.0
  path_provider: ^2.1.5
  record: ^6.2.0
  # RevenueCat — wraps Apple/Google IAP into one cross-platform SDK.
  # The public iOS key (appl_*) is safe to ship in source — RC validates
  # it against the app's bundle ID server-side, so a leaked key can't be
  # used by another app.
  purchases_flutter: ^8.0.0
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  go_router: ^17.1.0
  flutter_riverpod: ^3.2.1
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  pdf: ^3.11.1
  printing: ^5.13.2
  shared_preferences: ^2.5.4
  image_editor_plus: ^1.0.6
  image_picker: ^1.2.1
  file_picker: ^10.3.10
  photo_view: ^0.15.0
  cached_network_image: ^3.4.1
  mime: ^2.0.0
  http_parser: ^4.1.2
  flutter_math_fork: ^0.7.4
  web_socket_channel: ^3.0.3
  flutter_markdown: ^0.7.7+1
  markdown: ^7.3.0
  flutter_highlight: ^0.7.0
  flutter_pdfview: ^1.4.4
  flutter_local_notifications: ^21.0.0
  speech_to_text: ^7.0.0
  intl: any
  lottie: ^3.1.3
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  sentry_flutter: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.5
  sentry_dart_plugin: ^3.4.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/icon_light.png"
  # iOS rejects transparency in the home-screen icon. Flatten any
  # transparent pixels onto a solid background before generating.
  remove_alpha_ios: true
  background_color_ios: "#FFFFFF"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/icon_light.png"
  min_sdk_android: 21
  web:
    generate: false

flutter_native_splash:
  # White background + the blue CM icon so the native splash matches
  # the start of the Flutter Dart splash. No more black-flash gap on
  # cold launch.
  color: "#FFFFFF"
  color_dark: "#FFFFFF"
  image: assets/images/icon_light.png
  image_dark: assets/images/icon_light.png
  android: true
  ios: true
  android_12:
    color: "#000000"
    color_dark: "#000000"
    image: assets/images/icon_dark.png
    image_dark: assets/images/icon_dark.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/animations/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Sentry symbol/source upload (sentry_dart_plugin). Auth token is read from
# the SENTRY_AUTH_TOKEN env var at build time — never stored here.
sentry:
  upload_debug_symbols: true
  upload_sources: false
  upload_source_maps: false
  project: flutter
  org: classmate-02
  log_level: info
